# Version updates are handled by Renovate (see /renovate.json).
# Dependabot security updates and alerts remain enabled via
# Settings -> Security & quality -> Advanced Security.
# This file is intentionally kept with an empty updates list (official
# way to retire version updates while preserving the config for future
# security-updates grouping rules).
version: 2
updates: []
